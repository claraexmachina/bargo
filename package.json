{
  "name": "haggle",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "lint": "biome check .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.5.4",
    "tsx": "4.19.2",
    "vitest": "^2.1.8"
  }
}
